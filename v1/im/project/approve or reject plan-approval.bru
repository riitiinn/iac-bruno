meta {
  name: approve or reject plan-approval
  type: http
  seq: 1
}

post {
  url: {{url}}/v1/user/im/project/1/request-plan?status=approve
  body: json
  auth: bearer
}

params:query {
  status: approve
}

auth:bearer {
  token: {{user_token}}
}

body:json {
  {
  "reason":"not nice"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
